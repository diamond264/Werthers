{
  "name": "werthers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "ejs": "^2.5.7",
    "express": "^4.16.2",
    "mongoose": "^5.0.7",
    "multer": "^1.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  },
  "scripts": {
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src --out-dir build --presets=es2015",
    "development": "NODE_ENV=development nodemon --exec babel-node --presets=es2015 ./src/app.js --watch ./src",
    "start": "node ./build/app.js"
  },
  "author": "",
  "license": "ISC"
}
